name: lootkeeper_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Gerenciamento de estado
  provider: ^6.1.1

  # Câmera e scanner ISBN/QR
  mobile_scanner: ^5.2.3

  # Banco de dados local
  sqflite: ^2.3.2
  path: ^1.9.0

  # Requisições HTTP
  http: ^1.2.1

  # Utilidades
  cached_network_image: ^3.3.1
  shared_preferences: ^2.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true